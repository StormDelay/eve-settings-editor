[package]
name = "settings-model"
version = "0.9.0"
edition = "2021"
license = "MIT"

[dependencies]
blue-marshal = { path = "../blue-marshal" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
