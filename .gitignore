# Real EVE settings files and experiment dumps/diffs — personal data, never commit
testdata/

# Build outputs
target/
node_modules/
dist/

# SDD progress ledger (scratch)
.superpowers/

# Read-only reference clone of ntt/reverence (Task 4)
vendor/

# Scratch test logs (subagents sometimes redirect cargo test output)
test-output.txt
